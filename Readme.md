# Beer Wednesday Bot

Телеграм-бот, который принимает фото пива из чата, отправляет его в Groq Cloud (модели `llava-v1.5-7b` или `llama-3.2-vision`) и возвращает ироничный отзыв сомелье.

## Возможности

- Команды `/start` и `/help` объясняют, как пользоваться ботом.
- Принимает фотографии (с подписью или без неё) и отправляет их в Groq Vision API.
- Возвращает короткий ироничный отзыв о присланном напитке.
- Вежливо реагирует на текстовые сообщения без фото.

## Быстрый старт

1. **Создайте бота у @BotFather** и получите токен.
2. **Получите API-ключ Groq Cloud** на [console.groq.com](https://console.groq.com/) и убедитесь, что у вас есть доступ к модели `llava-v1.5-7b` или `llama-3.2-vision`.
3. Скопируйте `.env.example` в `.env` и заполните значения:
   ```bash
   cp .env.example .env
   ```
   Файл `.env` автоматически подхватится при запуске. Если предпочитаете задавать
   переменные напрямую в терминале, убедитесь, что вы экспортируете их в окружение,
   например:
   ```bash
   export TELEGRAM_BOT_TOKEN="1234567890:ABCDEF"
   export GROQ_API_KEY="gsk_..."
   ```
4. Установите зависимости и запустите бота:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
   pip install -r requirements.txt
   python -m beer_bot.main
   ```
5. Добавьте бота в дружеский чат и отправьте ему фото.

## Деплой на Railway

1. Установите [Railway CLI](https://docs.railway.com/develop/cli) и выполните `railway login`.
2. Создайте новый проект и подключите репозиторий (`railway init`).
3. Railway автоматически подхватит `requirements.txt` и `railway.json`.
4. Задайте переменные окружения через `railway variables set` или в веб-интерфейсе:
   - `TELEGRAM_BOT_TOKEN`
   - `GROQ_API_KEY`
   - (опционально) `GROQ_MODEL`, `GROQ_BASE_URL`, `GROQ_TEMPERATURE`, `GROQ_MAX_TOKENS`
   > ⚠️ **Важно.** Railway чувствителен к регистру имён переменных. Используйте заглавные
   > буквы точно как указано и не добавляйте кавычки вокруг значений. Проверить, что
   > значения применились, можно командой `railway variables list` или кнопкой «Reveal value»
   > в панели управления.
5. Запустите деплой командой `railway up` или через кнопку Deploy в панели управления.

При старте Railway выполнит `python -m beer_bot.main`, и бот начнёт опрашивать Telegram.

## Устранение неполадок

- **RuntimeError: "Отсутствуют обязательные переменные окружения".** Проверьте, что
  вы либо заполнили файл `.env`, либо экспортировали переменные окружения с помощью
  `export TELEGRAM_BOT_TOKEN=...` и `export GROQ_API_KEY=...` (на Windows используйте
  `setx` или `$env:`). Без экспорта оболочка не передаёт значения дочерним процессам.

## Переменные окружения

| Переменная | Описание |
| --- | --- |
| `TELEGRAM_BOT_TOKEN` | Токен бота из @BotFather. |
| `GROQ_API_KEY` | API ключ Groq Cloud. |
| `GROQ_MODEL` | (Опционально) Название модели Groq, по умолчанию `llama-3.2-vision`. |
| `GROQ_BASE_URL` | (Опционально) URL эндпоинта Chat Completions. |
| `GROQ_TEMPERATURE` | (Опционально) Температура сэмплинга ответа. |
| `GROQ_MAX_TOKENS` | (Опционально) Максимальное число токенов в ответе. |

## Структура проекта

```
beer_bot/
├── __init__.py
├── config.py         # чтение переменных окружения
├── groq_client.py    # обёртка над Groq Vision API
├── handlers.py       # обработчики Telegram-сообщений
└── main.py           # точка входа бота
```

## Разработка

- Для авто-подгрузки переменных из `.env` используется `python-dotenv`.
- Логирование настроено на уровень `INFO`. При ошибках обращения к Groq бот отправляет дружелюбное сообщение в чат и пишет стек в логи.

Удачных дегустаций по средам! 🍻
